export interface Book {
    id: number;
    author: string;
    title: string;
    quantity: string;
}

export interface Reader {
    id: number;
    firstName: string;
    lastName: string;
    phone: string;
}